import string
from codecs import encode as _dont_use_this_


def rot13(message):
    res=''
    for x in message:
        if 65 <= ord(x) <= 90:
            if ord(x) + 13 > 90:
                res =+ 
    pass
    
    If user select Yes, BIOS remove the SIID key so that the message will not appear again.